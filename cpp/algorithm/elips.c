/*
 * elips.c
 *  椭圆曲线y^2=x(x-1)(x-lambda), lambda !=0, 1
 *  椭圆曲线是域上亏格为1的光滑射影曲线，它的(仿射)方程，通常称为维尔斯特拉斯方程，可以写成
 *  Created on: Oct 17, 2024
 *      Author: rd
 */
#include <stdio.h>
#include <math.h>
int main() {
	int a =1;
	int b =1;
	double x;
	double y=sqrt(x(x-a)(x-b));
	for(double i = 0; i+=0.1; i<100) {

	}
	return 0;
}

